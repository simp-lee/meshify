# Meshify-managed: app.name={{ .AppName }}
# Source: deploy/templates/app/lego-renew.timer.tmpl

[Unit]
Description=Run {{ .AppName }} lego renewal twice daily

[Timer]
OnCalendar=*-*-* 03,15:17:00
RandomizedDelaySec=45m
Persistent=true

[Install]
WantedBy=timers.target
